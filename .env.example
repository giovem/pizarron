# Copia este archivo a .env y rellena SUPABASE_ANON_KEY con tu anon key de Supabase (API).
# Luego en Netlify: Environment variables → Add from .env → sube o pega el contenido de .env
# No subas .env a GitHub (está en .gitignore)

SUPABASE_URL=https://tu-proyecto.supabase.co
SUPABASE_ANON_KEY=tu_anon_key_jwt_aqui
