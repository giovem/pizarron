# Cloudflare Workers: sitio estático (assets). Build: npm run build → inject-env.js
name = "pizarronepem"
compatibility_date = "2026-02-19"

[assets]
directory = "."
# Qué no subir: .assetsignore (mismo formato que .gitignore)
